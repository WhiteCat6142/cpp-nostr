{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "ninja",
            "args": [
                "-v"
            ],
            "options": {
                "cwd": "${workspaceFolder}/build/"
            },
            "group": "build"
        }
    ]
}